ssh-keygen -t ed25519 -C "palak@example.com"
ls -al ~/.ssh
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
Copy the entire output and add it to GitHub:
Go to GitHub SSH Keys Settings
Click New SSH key
Give it a title (e.g., "Palak MacBook Air")
Paste the key
Save
 # modify check


 Git commands for new repo:
 Step1: Creae a repository on github account
 On local system: 
 Step2: come on the correct directory
 Step3: git clone repository_address 
  eg: git clone git@github.com:palakverma0110-pixel/network-security-scanner.git
Step4: changes(Add or modification) on local system in the cloned repository
Step5: Add changes on git
  eg: git add file_name
ste6: git commit -m "message"
Step7: git push origin main 


